# 墨启 MOQI 产品路线

## 产品定位

墨启不是“带终端的普通笔记”，而是面向运维、网络和 IT 团队的轻量智能工作台：

- 笔记：个人知识沉淀、团队文档、表格资料和实时协作。
- 连接：统一管理网络设备、Linux、Windows 等远程会话。
- AI：在笔记、终端输出、设备状态和自动化报告之间提供辅助。
- 自动化：在服务器端按计划执行任务，即使桌面应用未打开也能工作。

## P0：先建立可信的个人与团队边界

这是开放给更多同事使用前必须完成的基础，不应只依靠前端隐藏按钮。

### 空间模型

- 我的空间：仅本人可见；默认所有新笔记都创建在这里。
- 团队空间：团队成员可见；文档可指定查看者、编辑者和管理员。
- 发布动作：个人笔记只有经过明确的“共享到团队”操作才进入团队空间。
- 共享会话：设备会话默认私有，可共享给指定同事或团队分组。

### 服务端强制权限

- 新增组织、成员、空间、角色和权限表。
- 每个文档与会话记录都保存 `owner`、`space` 和权限，不再使用所有用户共用的一棵全局树。
- REST、搜索、附件和实时协作连接都在服务器端校验权限。
- Yjs 实时协作房间只有拥有查看或编辑权限的用户才能加入；只读成员不能提交更新。
- 操作日志记录谁在何时查看、修改、共享或执行了设备任务。
- 设备密码、私钥口令和 API Key 只加密存储，不通过会话导出文件传播。

## P1：笔记体验

- 简洁活动栏、现代 Windows 一体化标题栏。
- 浅色、深色、书页护眼三套主题。
- `/` 命令菜单提供可见搜索，可用“一级”“H1”等关键词搜索。
- Excel、CSV、TSV 导入为笔记中的可编辑表格。
- 实时协作支持多人在线、光标与在线成员显示。
- 后续：文档评论、提及、修改历史、只读分享链接和表格筛选。

## P2：远程会话工作台

- 双击设备连接，多标签并行会话，按分组搜索。
- 导入 SecureCRT `.ini`、MobaXterm `.mxtsessions` 和墨启会话文件。
- 导出墨启会话文件时不包含密码和私钥口令。
- 后续增加：会话属性面板、命令栏、日志检索、批量执行、端口转发和分屏终端。
- 团队共享会话只同步连接参数和权限；敏感凭据可由管理员托管，也可要求每位成员单独填写。

## P3：AI 快捷助手

- 应用内按 `Ctrl+Space` 打开悬浮小窗，再次按下或按 Esc 关闭。
- 可选择是否把当前笔记或终端片段作为上下文。
- 后续增加可配置快捷键；如需在应用未聚焦时全局唤起，再增加独立的系统级快捷窗口。
- AI 结果进入笔记前由用户确认；设备配置命令默认只建议，不自动执行。

## P4：服务器自动化平台

### 组成

- 调度器：保存一次性、每天、每周和 Cron 计划。
- Worker：在服务器端执行 SSH、Telnet、WinRM 或脚本任务，桌面应用无需在线。
- 凭据库：加密保存设备凭据，按任务和成员授权。
- 报告器：整理执行结果，调用 AI 分析，生成结构化报告。
- 通知器：邮件、应用内通知；后续可接企业微信、飞书或 Teams。

### 安全规则

- 高风险命令支持审批、维护窗口和最大并发限制。
- 每次执行保存任务版本、目标设备、命令、输出、发起人和结果。
- 支持超时、重试、失败停止、部分成功和人工取消。
- AI 不直接获得无限制终端权限；生成命令与执行权限分离。

## P5：面向其他用户的内网部署

- 提供 Docker Compose 安装包，包含 API、实时协作、调度 Worker、数据库和反向代理。
- 每个客户使用独立的数据与密钥；支持备份、恢复、健康检查和离线升级包。
- 外部访问统一使用域名与 HTTPS。优先使用企业 VPN、零信任访问或安全隧道，不直接暴露数据库和设备管理端口。
- 桌面应用只连接客户自己的墨启服务器；管理员可控制版本通道和成员注册策略。

## 发布顺序

1. 完成 P0 权限隔离并迁移现有共享数据。
2. 发布本轮 P1/P2/P3 界面和导入能力，进行 2～3 人协作测试。
3. 强化终端共享、审计和凭据管理。
4. 实现服务器调度、Worker、报告和通知的最小闭环。
5. 制作可交付的内网部署与升级方案，再开放给外部用户。
